{
  "name": "slurm-mon",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install"
  },
  "dependencies": {
    "bower": "*",
    "express.io": "*"
  }
}
